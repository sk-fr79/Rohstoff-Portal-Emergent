	/**
     * References the Field #DAUGHTERFIELD# 
     */
	public MAP_#DAUGHTERTABLEBASE# get_DOWN_MAP_#NAME_OF_DAUGHTERMAP#() throws myException
	{
		if (vIDs.size()!=1)
			throw new myException(this,"Method only in singular MAP-Objects allowed ");
		
		
		if (this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(this.get_cUF_ID_#TABLEBASE#())==null)
		{
			this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.put(this.get_cUF_ID_#TABLEBASE#(),new MAP_#DAUGHTERTABLEBASE#("#DAUGHTERFIELD#="+this.get_cUF_#MOTHERFIELD#(),this.oConn));
			
		}
		return this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(this.get_cUF_ID_#TABLEBASE#());
	}
	
	
	/**
     * References the Field #DAUGHTERFIELD# 
     */
	public MAP_#DAUGHTERTABLEBASE# get_DOWN_MAP_#NAME_OF_DAUGHTERMAP#(String cID) throws myException
	{
		if (!this.vIDs.contains(cID))
			throw new myException(this, "ID is not in the collection");
		
		if (this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(cID)==null)
		{
			this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.put(cID,new MAP_#DAUGHTERTABLEBASE#("#DAUGHTERFIELD#="+this.get_cUF_#MOTHERFIELD#(cID),this.oConn));
			
		}
		return this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(this.get_cUF_ID_#TABLEBASE#(cID));
	}


	/**
     * References the Field #DAUGHTERFIELD# 
     */
	public MAP_#DAUGHTERTABLEBASE# get_DOWN_MAP_#NAME_OF_DAUGHTERMAP#(int iPos)  throws myException
	{
		if (iPos < 0)
			throw new myException(this, "Number MUST be greater equal 0 ");

		if (iPos >= this.vIDs.size())
			throw new myException(this, "Number of Row smaller than " + iPos);

		String cID = this.vIDs.get(iPos);

		if (this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(cID)==null)
		{
			this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.put(cID,new MAP_#DAUGHTERTABLEBASE#("#DAUGHTERFIELD#="+this.get_cUF_#MOTHERFIELD#(cID),this.oConn));
			
		}
		return this.hmMAP_DOWN_#NAME_OF_DAUGHTERMAP#.get(this.get_cUF_ID_#TABLEBASE#(cID));
		
	}
